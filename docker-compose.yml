version: "3"
services:
  mongo:
    image: mongo
    restart: always
    ports: 
      - 3000:27017
  back-end-main:
    image: node:latest
    working_dir: /back-end-main
    command: npm start
    ports:
      - 3001:1337
    environment:
      - backend.env
    links:
      - mongo